<!-- Modal Unifié avec Transitions -->
<div class="modal fade" id="unifiedModal" tabindex="-1" aria-labelledby="unifiedModalLabel" aria-hidden="true" data-bs-backdrop="static">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content unified-modal">
            <!-- Header avec bouton fermer -->
            <div class="modal-header border-0 p-3" id="modal-header-unified">
                <h5 class="modal-title" id="unifiedModalLabel">Modal</h5>
                <button type="button" class="btn-close" onclick="closeUnifiedModal()" aria-label="Close"></button>
            </div>
            
            <!-- Container des vues avec transitions -->
            <div class="modal-body p-0" id="modal-views-container">
                
                <!-- Vue 1: Détails de la blague -->
                <div class="modal-view active" id="view-joke-detail" data-view="joke-detail" >
                    <div class="joke-detail-container" >
                        <!-- Partie gauche: Informations -->
                        <div class="joke-detail-info">
                            <div class="joke-detail-header">
                                <div class="joke-detail-extra-image" style="text-align: center; margin: 20px 0;">
                                    <img id="detail-extra-image" src="{{asset('images/test.png')}}" alt="Image supplémentaire" style="max-width: 100%; height: auto; display: inline-block;" />
                                </div>
                                <span class="joke-category" style="background-color: #ccc" id="modal-detail-category">
                                    Non catégorisé
                                </span>
                                <h3 class="joke-title" id="detail-title">Titre de la blague</h3>
                                <p class="joke-description" id="detail-description">Description de la blague...</p>
                            </div>
                            
                            <div class="joke-detail-footer">
                                <div class="joke-price-section">
                                    <span class="joke-price" id="detail-price">€9.99</span>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Partie droite: Image -->
                        <div class="joke-detail-image">
                            <img id="detail-image" src="" alt="" />
                        </div>
                    </div>
                </div>

                <!-- Vue 2: Paiement - Étape Carte -->
                <div class="modal-view" id="view-payment-card" data-view="payment-card" style="display: none;">
                    <div class="payment-view-container">
                        <!-- Bannière en cover au top qui couvre tout -->
              
                        
                        <div class="payment-content p-4">
                            <form id="payment-form">
                                <div class="row">
                                    <div class="col-12 mb-3">
                                        <label for="email" class="form-label">Email *</label>
                                        <input type="email" class="form-control" id="email" 
                                               value="{{ app.user ? app.user.email : '' }}" required>
                                        <div class="invalid-feedback" id="email-error"></div>
                                        <small class="form-text text-muted">La blague sera envoyée à cette adresse</small>
                                    </div>
                                </div>
                                
                                <!-- Stripe Elements Container -->
                                <div class="row">
                                    <div class="col-12 mb-3">
                                        <label class="form-label">Informations de carte *</label>
                                        <div id="card-element" class="stripe-element">
                                            <!-- Stripe Elements will create form elements here -->
                                        </div>
                                        <div id="card-errors" role="alert" class="invalid-feedback"></div>
                                    </div>
                                </div>
                                
                                <!-- Nom sur la carte -->
                                <div class="row">
                                    <div class="col-12 mb-3">
                                        <label for="card-holder-name" class="form-label">Nom sur la carte *</label>
                                        <input type="text" class="form-control" id="card-holder-name" required>
                                        <div class="invalid-feedback" id="card-holder-name-error"></div>
                                    </div>
                                </div>
                                
                                <div class="payment-test-info">
                                    <small class="text-success">
                                        <i class="fas fa-info-circle me-1"></i>
                                        Test: 4242 4242 4242 4242 | 12/28 | 123
                                    </small>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>

                <!-- Vue 3: Paiement - Étape Facturation -->
                <div class="modal-view" id="view-payment-billing" data-view="payment-billing" style="display: none;">
                    <div class="payment-view-container">
                        <div class="payment-content p-4">
                            <div class="payment-header mb-4">
                                <h6 class="step-title">📍 Adresse de facturation</h6>
                                <p class="step-subtitle text-muted">Informations requises pour la facturation</p>
                            </div>
                            
                            <form id="billing-form">
                                <div class="row">
                                    <div class="col-12 mb-3">
                                        <label for="country" class="form-label">Pays *</label>
                                        <select class="form-select" id="country" required>
                                            <option value="">Sélectionner un pays</option>
                                            <option value="FR" selected>France</option>
                                            <option value="BE">Belgique</option>
                                            <option value="CH">Suisse</option>
                                            <option value="CA">Canada</option>
                                            <option value="US">États-Unis</option>
                                        </select>
                                        <div class="invalid-feedback" id="country-error"></div>
                                    </div>
                                </div>
                                
                                <div class="row">
                                    <div class="col-12 mb-3">
                                        <label for="address" class="form-label">Adresse *</label>
                                        <input type="text" class="form-control" id="address" 
                                               value="{{ app.user ? app.user.address : '' }}"
                                               list="address-suggestions" required>
                                        <datalist id="address-suggestions"></datalist>
                                        <div class="invalid-feedback" id="address-error"></div>
                                    </div>
                                </div>
                                
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label for="city" class="form-label">Ville *</label>
                                        <input type="text" class="form-control" id="city" required>
                                        <div class="invalid-feedback" id="city-error"></div>
                                    </div>
                                    <div class="col-md-3 mb-3">
                                        <label for="region" class="form-label">Région</label>
                                        <input type="text" class="form-control" id="region">
                                        <div class="invalid-feedback" id="region-error"></div>
                                    </div>
                                    <div class="col-md-3 mb-3">
                                        <label for="postal-code" class="form-label">Code postal *</label>
                                        <input type="text" class="form-control" id="postal-code" required>
                                        <div class="invalid-feedback" id="postal-code-error"></div>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>

                <!-- Vue 4: Traitement -->
                <div class="modal-view" id="view-payment-processing" data-view="payment-processing" style="display: none;">
                    <div class="payment-view-container">
                        <div class="payment-content text-center py-5">
                            <div class="spinner-border text-primary mb-3" style="width: 3rem; height: 3rem;" role="status">
                                <span class="visually-hidden">Traitement...</span>
                            </div>
                            <h6 class="step-title">🔒 Traitement du paiement</h6>
                            <p class="text-muted">Traitement sécurisé en cours...</p>
                            <small class="text-muted">Ne fermez pas cette fenêtre</small>
                        </div>
                    </div>
                </div>

                <!-- Vue 5: Succès -->
                <div class="modal-view" id="view-payment-success" data-view="payment-success" style="display: none;">
                    <div class="payment-view-container">
                        <div class="payment-content text-center py-4">
                            <div class="success-animation mb-4">
                                <i class="fas fa-check-circle text-success" style="font-size: 4rem;"></i>
                            </div>
                            <h4 class="text-success mb-3">🎉 Paiement réussi !</h4>
                            <div class="success-details">
                                <div class="alert alert-success">
                                    <h6 class="mb-2">Votre blague a été envoyée !</h6>
                                    <p class="mb-1">
                                        <i class="fas fa-envelope me-2"></i>
                                        <strong id="success-email"></strong>
                                    </p>
                                    <small class="text-muted">Vérifiez votre boîte de réception et vos spams</small>
                                </div>
                                <div class="joke-preview p-3 border rounded">
                                    <h6 id="success-joke-title" class="mb-2"></h6>
                                    <p class="text-muted mb-0">Commande #<span id="success-order-id"></span></p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Vue 6: Erreur -->
                <div class="modal-view" id="view-payment-error" data-view="payment-error" style="display: none;">
                    <div class="payment-view-container">
                        <div class="payment-content text-center py-4">
                            <div class="error-animation mb-4">
                                <i class="fas fa-times-circle text-danger" style="font-size: 4rem;"></i>
                            </div>
                            <h4 class="text-danger mb-3">❌ Erreur de paiement</h4>
                            <div class="alert alert-danger">
                                <p id="error-message" class="mb-0"></p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Footer avec navigation -->
            <div class="modal-footer" id="modal-footer-unified">
                <!-- Informations du produit -->
                <div class="product-info d-flex align-items-center justify-content-between w-100" id="product-info-section">
                    <div class="product-price" id="footer-product-price">
                        <i class="fas fa-tag me-1"></i><span class="fw-bold text-success">0.99€</span>
                    </div>
                    <div class="product-title" id="footer-product-title">
                        <i class="fas fa-laugh me-1"></i><span>Titre de la blague</span>
                    </div>
                </div>
                
                <!-- Boutons de navigation -->
                <div class="modal-actions" id="modal-actions-section">
                    <button type="button" class="btn btn-danger" onclick="closeUnifiedModal()">
                        <i class="fas fa-times me-1"></i>Annuler
                    </button>
                    <button type="button" class="btn btn-outline-secondary" id="back-btn-unified" onclick="goToPreviousView()" style="display: none;">
                        <i class="fas fa-arrow-left me-1"></i>Retour
                    </button>
                    <button type="button" class="btn btn-primary" id="next-btn-unified" onclick="goToNextView()">
                        Continuer
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- CSS pour les transitions -->
<style>
.unified-modal {
    overflow: hidden;
    transition: all 0.3s ease; /* Transition fluide pour les changements de taille */
}

.unified-modal .modal-content {
    transition: all 0.3s ease; /* Transition fluide pour le contenu */
}

#modal-views-container {
    position: relative;
    min-height: 300px; /* Hauteur minimum réduite */
    transition: height 0.3s ease; /* Transition fluide pour les changements de hauteur */
}

.modal-view {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    min-height: 400px;
    opacity: 1;
    transform: translateX(0);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.modal-view.slide-out-left {
    opacity: 0;
    transform: translateX(-100%);
}

.modal-view.slide-out-right {
    opacity: 0;
    transform: translateX(100%);
}

.modal-view.slide-in-left {
    opacity: 0;
    transform: translateX(-100%);
}

.modal-view.slide-in-right {
    opacity: 0;
    transform: translateX(100%);
}

.modal-view.active {
    opacity: 1;
    transform: translateX(0);
    position: relative;
}

.payment-view-container {
    min-height: 400px;
}

.payment-content {
    min-height: 350px;
}

.joke-detail-container {
    display: flex;
    min-height: 400px;
}

.joke-detail-info {
    flex: 1;
    padding: 2rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

.joke-detail-image {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #f8f9fa;
    min-height: 300px; /* Hauteur minimum pour éviter que l'image soit trop petite */
}

.joke-detail-image img {
    width: 100%;
    height: 300px; /* Hauteur fixe pour l'image */
    object-fit: cover;
    border-radius: 8px;
}

/* Animation d'entrée pour le modal */
.unified-modal.show {
    animation: modalSlideIn 0.3s ease-out;
}

@keyframes modalSlideIn {
    from {
        opacity: 0;
        transform: scale(0.95) translateY(-20px);
    }
    to {
        opacity: 1;
        transform: scale(1) translateY(0);
    }
}

/* Masquer le carré jaune spécifiquement dans la vue détails */
.modal.view-joke-detail #product-info-section {
    display: none !important;
}

/* Quand on est sur la vue joke-detail, espacer les boutons aux extrémités */
.modal.view-joke-detail #modal-footer-unified {
    justify-content: space-between !important;
    width: 100%;
}

.modal.view-joke-detail #modal-actions-section {
    width: 100%;
    justify-content: space-between;
}

/* Styles pour la vue détails */
.joke-detail-footer {
    display: flex;
    justify-content: flex-start;
    align-items: center;
    margin-top: auto;
}

.joke-price-section {
    display: flex;
    justify-content: flex-start;
    align-items: center;
}

.joke-price {
    font-size: 1.5rem;
    font-weight: bold;
    color: #28a745;
}

/* Supprimer les dividers visuels */
.joke-detail-header {
    border: none;
}

 

/* Styles pour les étapes de paiement */
.payment-header {
    border-bottom: 1px solid #dee2e6;
    padding-bottom: 1rem;
}

.step-title {
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0;
}

.step-subtitle {
    font-size: 0.9rem;
    margin: 0;
}

 

.banner-full-cover {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 8px 8px 0 0;
}

/* Ajuster le contenu du paiement */
.payment-view-container {
    min-height: 400px;
    overflow: hidden;
    border-radius: 8px; /* Assurer l'arrondi */
}

.payment-content {
    min-height: 350px;
    position: relative;
    z-index: 1; /* Au-dessus de la bannière */
}

/* Responsive */
@media (max-width: 768px) {
    .joke-detail-container {
        flex-direction: column;
    }
    
    .joke-detail-info {
        padding: 1rem;
    }
    
    .payment-content {
        padding: 1rem !important;
    }
    
    
}
</style>
